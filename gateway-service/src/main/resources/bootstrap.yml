spring:
  cloud:
    config:
      name: gateway-service
      discovery:
        enabled: true
        service-id: config-serivce

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8082/eureka/