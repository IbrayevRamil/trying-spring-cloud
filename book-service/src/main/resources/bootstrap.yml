spring:
  cloud:
    config:
      name: book-service
      discovery:
        enabled: true
        service-id: config-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8082/eureka/